const menu =document.querySelector("nav-link")
document.addEventListener("DOMContentLoaded", function (e) {
    console.log(e.target.trim == "http://localhost:5111/");
    if (e.target =="http://localhost:5111/" ) {

    } else {

    }
});